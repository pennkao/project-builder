// Header.tsx

import Footer from "./Footer";

// Footer.tsx
const Header = () => {
    return (
        <header>
            <p>Admin Layout Header</p>
        </header>
    );
};

export default Header;
